source: lib_author.yaml
type: database
seed:
  type: embedded
  value: 42
conf:
  jdbc_url: "jdbc:postgresql://localhost:5432/librarydb"
  username: "dbuser"
  password: "${DB_PASSWORD}"
  table: "lib_authors"
  batch_size: 50
  pool_size: 2
  transaction_strategy: per_batch
  inject_parent_fk: false
