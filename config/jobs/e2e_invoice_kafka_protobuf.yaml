# End-to-End Benchmark: Invoice → Kafka → Protobuf
# Used for E2E performance testing with nested object structure

source: invoice.yaml
type: kafka
seed:
  type: embedded
  value: 42
conf:
  bootstrap: localhost:9092
  topic: benchmark-topic
  sync: false
  batchSize: 16384
  compression: snappy
