# End-to-End Regex Benchmark: BASELINE (char[] instead of regex) → File → CSV
# Control run for e2e_regex_file_csv.yaml — see config/structures/regex_reference_baseline.yaml
# Driven by benchmarks/run_regex_e2e.sh

source: regex_reference_baseline.yaml
type: file
seed:
  type: embedded
  value: 42
conf:
  path: build/run-output/e2e_regex_baseline_file_csv
